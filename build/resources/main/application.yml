spring:
  application:
    name: book-ecommerce

  datasource:
    url: jdbc:mysql://localhost:3306/book_ecommerce
    username: root
    password: root
  jpa:
    hibernate:
      ddl-auto: create
    show-sql: true
